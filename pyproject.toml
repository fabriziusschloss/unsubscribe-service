[project]
name = "unsubscribe-service"
version = "1.0.0"
description = "DSGVO Newsletter Abmelde-Service"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0.0",
    "requests>=2.31.0",
    "gunicorn>=21.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
]
